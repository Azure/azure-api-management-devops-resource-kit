jobs:
- job: Build
  pool:
    vmImage: 'VSEngSS-MicroBuild2022-1ES'
  
  steps:
  - template: common/build-and-test.yml

trigger:
  branches:
    include:
    - '*'

pr:
  branches:
    include:
    - '*'